[project]
name = "EMP"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flet>=0.85.1",
    "flet_audio>=0.85.1",
    "mutagen>=1.47.0"
]

[tool.flet]
desktop_flavor = "full"

[tool.flet.app]
path = "src"
